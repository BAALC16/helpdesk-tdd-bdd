{
  "name": "helpdesk-tdd-bdd",
  "version": "1.0.0",
  "description": "Système de gestion de tickets avec TDD/BDD",
  "main": "src/ticket.js",
  "scripts": {
    "test": "jest --coverage --passWithNoTests",
    "test:watch": "jest --watch",
    "test:bdd": "jest --testMatch='**/features/**/*.steps.js'",
    "lint": "eslint src tests --max-warnings=0 || true",
    "lint:fix": "eslint src tests --fix"
  },
  "keywords": ["tdd", "bdd", "helpdesk", "agile"],
  "author": "Votre Nom",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "jest-cucumber": "^3.0.1"
  }
}
